<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Obama Naiji Background Remover</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2a2a2a;
            --accent: #00ff88;
            --dark: #1a1a1a;
            --light: #f5f5f5;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', sans-serif;
        }

        body {
            background: var(--dark);
            color: var(--light);
            min-height: 100vh;
        }

        .header {
            background: rgba(42, 42, 42, 0.95);
            padding: 1rem 5%;
            position: fixed;
            width: 100%;
            top: 0;
            z-index: 1000;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(0, 255, 136, 0.1);
        }

        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--accent);
            text-decoration: none;
            animation: slideIn 0.5s ease-out;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
        }

        .nav-links a {
            color: var(--light);
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .nav-links a:hover {
            color: var(--accent);
        }

        .hero {
            padding: 120px 5% 60px;
            text-align: center;
        }

        .hero h1 {
            font-size: 3rem;
            margin-bottom: 1rem;
            background: linear-gradient(45deg, #00ff88, #00ccff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: fadeInUp 0.8s ease-out;
        }

        .samples-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            padding: 2rem 5%;
        }

        .sample-card {
            background: var(--primary);
            border-radius: 15px;
            overflow: hidden;
            transform: translateY(0);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            cursor: pointer;
        }

        .sample-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0,255,136,0.1);
        }

        .sample-img {
            width: 100%;
            height: 250px;
            object-fit: cover;
            border-bottom: 2px solid var(--accent);
        }

        .guide-section {
            padding: 4rem 5%;
            background: rgba(42, 42, 42, 0.5);
            margin: 2rem 0;
        }

        .guide-steps {
            display: grid;
            gap: 2rem;
            counter-reset: step-counter;
        }

        .guide-step {
            padding: 2rem;
            background: var(--primary);
            border-radius: 15px;
            position: relative;
            counter-increment: step-counter;
        }

        .guide-step::before {
            content: counter(step-counter);
            position: absolute;
            left: -20px;
            top: -20px;
            background: var(--accent);
            color: var(--dark);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
        }

        .generator-section {
            padding: 4rem 5%;
        }

        .generator-box {
            max-width: 800px;
            margin: 0 auto;
            background: var(--primary);
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .image-preview {
            width: 100%;
            max-width: 600px;
            margin: 1rem auto;
            position: relative;
            border: 2px dashed var(--accent);
            border-radius: 15px;
            padding: 1rem;
        }

        .preview-image {
            width: 100%;
            max-height: 400px;
            object-fit: contain;
            border-radius: 10px;
        }

        .file-input {
            display: none;
        }

        .upload-btn {
            background: var(--accent);
            color: var(--dark);
            padding: 1rem 2rem;
            border-radius: 50px;
            cursor: pointer;
            display: inline-block;
            margin: 1rem 0;
        }

        .comparison-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            margin: 2rem 0;
        }

        .generate-btn, .download-btn {
            background: var(--accent);
            color: var(--dark);
            border: none;
            padding: 1rem 2rem;
            border-radius: 50px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            display: inline-block;
        }

        .generate-btn:hover, .download-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0,255,136,0.3);
        }

        .footer {
            background: var(--primary);
            padding: 2rem 5%;
            text-align: center;
            margin-top: 4rem;
        }

        .loading {
            display: none;
            margin: 2rem 0;
            text-align: center;
        }

        .loading-dot {
            display: inline-block;
            width: 12px;
            height: 12px;
            margin: 0 4px;
            background: var(--accent);
            border-radius: 50%;
            animation: loading 1.4s infinite ease-in-out;
        }

        .loading-dot:nth-child(2) { animation-delay: 0.2s; }
        .loading-dot:nth-child(3) { animation-delay: 0.4s; }

        @media (max-width: 768px) {
            .nav-links {
                gap: 1.2rem;
                font-size: 0.9rem;
            }

            .hero {
                padding: 100px 5% 40px;
            }

            .hero h1 {
                font-size: 2rem;
                line-height: 1.3;
            }

            .samples-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .guide-step {
                padding: 1.5rem;
                margin-left: 30px;
            }

            .guide-step::before {
                left: -30px;
                top: 15px;
            }

            .generator-box {
                padding: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            .nav {
                flex-direction: column;
                gap: 1rem;
                align-items: flex-start;
            }

            .nav-links {
                flex-wrap: wrap;
                gap: 1rem;
            }

            .hero h1 {
                font-size: 1.75rem;
            }

            .guide-steps {
                grid-template-columns: 1fr;
            }

            .generate-btn, .download-btn {
                width: 100%;
                padding: 1rem;
            }

            .comparison-container {
                grid-template-columns: 1fr;
            }
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes slideIn {
            from { transform: translateX(-50px); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }

        @keyframes loading {
            0%, 80%, 100% { transform: scale(0); }
            40% { transform: scale(1); }
        }
    </style>
</head>
<body>
    <header class="header">
        <nav class="nav">
            <a href="#" class="logo">BgRemoverAI</a>
            <div class="nav-links">
                <a href="#samples">Samples</a>
                <a href="#guide">Guide</a>
                <a href="#generate">Remove BG</a>
            </div>
        </nav>
    </header>

    <section class="hero">
        <h1>Remove Image Backgrounds Instantly</h1>
        <p>Powered by AI Technology</p>
    </section>

    <section id="samples" class="samples-grid">
        <div class="sample-card">
            <div class="comparison-container">
                <div class="result-image">
                    <img src="https://images.unsplash.com/photo-1524504388940-b1c1722653e1" class="sample-img" alt="Original">
                    <div class="sample-info">
                        <h3>Original Image</h3>
                    </div>
                </div>
                <div class="result-image">
                    <img src="https://www.remove.bg/images/samples/combined/s1-thumbnail-removebg-preview.png" class="sample-img" alt="Processed">
                    <div class="sample-info">
                        <h3>Background Removed</h3>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section id="guide" class="guide-section">
        <h2>How It Works</h2>
        <div class="guide-steps">
            <div class="guide-step">
                <h3>Upload Your Image</h3>
                <p>Select any JPG/PNG image with clear subject</p>
            </div>
            <div class="guide-step">
                <h3>AI Processing</h3>
                <p>Our AI will automatically remove the background</p>
            </div>
            <div class="guide-step">
                <h3>Download Result</h3>
                <p>Get your transparent PNG image instantly</p>
            </div>
        </div>
    </section>

    <section id="generate" class="generator-section">
        <div class="generator-box">
            <h2>Remove Background Now</h2>
            <label class="upload-btn">
                Choose Image
                <input type="file" id="fileInput" class="file-input" accept="image/*">
            </label>
            
            <div class="image-preview" id="imagePreview">
                <img id="previewImage" class="preview-image">
            </div>
            
            <button class="generate-btn" onclick="processImage()">Remove Background</button>
            <div class="loading" id="loading">
                <div class="loading-dot"></div>
                <div class="loading-dot"></div>
                <div class="loading-dot"></div>
            </div>
            <div id="resultContainer"></div>
        </div>
    </section>

    <footer class="footer">
        <p>© Obama Naija AI. A product of Bitos. All rights reserved.</p>
    </footer>

    <script>
        // Get your free API key from https://deepai.org/
        const API_KEY = "79b7c1b7-135d-4352-907f-8596ead3ccb7";
        const API_URL = "https://api.deepai.org/api/background-removal";

        document.getElementById('fileInput').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (file) {
                const reader = new FileReader();
                reader.onload = function(e) {
                    document.getElementById('previewImage').src = e.target.result;
                }
                reader.readAsDataURL(file);
            }
        });

        async function processImage() {
            const fileInput = document.getElementById('fileInput');
            const loading = document.getElementById('loading');
            const resultContainer = document.getElementById('resultContainer');

            if (!fileInput.files[0]) {
                alert('Please select an image first');
                return;
            }

            try {
                loading.style.display = 'block';
                resultContainer.innerHTML = '';

                const formData = new FormData();
                formData.append('image', fileInput.files[0]);

                const response = await fetch(API_URL, {
                    method: 'POST',
                    headers: {
                        'api-key': API_KEY
                    },
                    body: formData
                });

                const data = await response.json();

                if (!response.ok) {
                    throw new Error(data.output || 'Failed to remove background');
                }

                // Fetch the result image
                const imageResponse = await fetch(data.output_url);
                const blob = await imageResponse.blob();
                const imageUrl = URL.createObjectURL(blob);

                resultContainer.innerHTML = `
                    <div class="result-card">
                        <div class="comparison-container">
                            <div class="result-image">
                                <img src="${document.getElementById('previewImage').src}" class="preview-image">
                                <p>Original Image</p>
                            </div>
                            <div class="result-image">
                                <img src="${imageUrl}" class="preview-image">
                                <p>Background Removed</p>
                            </div>
                        </div>
                        <button onclick="downloadImage('${imageUrl}')" class="download-btn">
                            Download PNG Image
                        </button>
                    </div>
                `;

            } catch (error) {
                alert(error.message);
            } finally {
                loading.style.display = 'none';
            }
        }

        function downloadImage(imageUrl) {
            const link = document.createElement('a');
            link.href = imageUrl;
            link.download = `bg-removed-${Date.now()}.png`;
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);
        }
    </script>
</body>
</html>
